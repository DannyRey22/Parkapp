

:root{
  --bg:#fffef6;
  --card:#ffffffcc;
  --accent:#00a86b;
  --accent-2:#ff7aa2;
  --muted:#6b6b6b;
  --radius:18px;
  --glass: rgba(255,255,255,0.7);
  font-family: Inter, ui-sans-serif, system-ui;
}

body{
  margin:0;
  background:linear-gradient(180deg,var(--bg),#f0fff7);
  color:#0b2a1a;
  overflow-x:hidden;
}


#loading-screen{
  position:fixed;
  top:0;left:0;right:0;bottom:0;
  background:var(--bg);
  display:flex;
  align-items:center;
  justify-content:center;
  flex-direction:column;
  gap:18px;
  z-index:9999;
  animation:fadeOut 0.6s ease forwards;
  animation-delay:1.8s;
}

.spinner{
  width:56px;
  height:56px;
  border-radius:50%;
  border:6px solid var(--accent);
  border-top-color:transparent;
  animation:spin 0.9s linear infinite;
}

@keyframes spin{to{transform:rotate(360deg);}}
@keyframes fadeOut{to{opacity:0;visibility:hidden;}}


header{padding:28px 18px;display:flex;align-items:center;gap:18px;justify-content:space-between}
.brand{display:flex;gap:14px;align-items:center}
.logo{width:68px;height:68px;border-radius:14px;background:linear-gradient(135deg,var(--accent),var(--accent-2));display:flex;align-items:center;justify-content:center;box-shadow:0 8px 30px rgba(0,0,0,0.08)}
.logo svg{filter:drop-shadow(0 6px 18px rgba(0,0,0,0.12))}
h1{font-size:20px;margin:0}
p.tag{margin:0;font-size:12px;color:var(--muted)}

.controls{display:flex;gap:12px;align-items:center}
.btn{background:var(--accent);color:white;padding:10px 14px;border-radius:12px;border:none;font-weight:600;cursor:pointer;box-shadow:0 6px 18px rgba(0,0,0,0.08);transition:0.2s}
.btn:hover{transform:scale(1.05)}
.btn.ghost{background:transparent;border:2px solid rgba(11,42,26,0.08);color:var(--accent)}

main{display:grid;grid-template-columns:1fr 420px;gap:20px;padding:18px;align-items:start}
#map{width:100%;height:70vh;border-radius:18px;box-shadow:0 12px 40px rgba(0,0,0,0.08);overflow:hidden}

aside{position:sticky;top:22px;height:calc(100vh - 44px);padding:18px;background:var(--card);border-radius:18px;box-shadow:0 8px 24px rgba(0,0,0,0.06);overflow:auto}

.search{display:flex;gap:8px;margin-bottom:12px}
.search input{flex:1;padding:10px 12px;border-radius:10px;border:2px solid rgba(11,42,26,0.06);font-weight:600}


/* Animación "aparecer" para parques */
.park{display:flex;gap:12px;align-items:center;background:linear-gradient(180deg,#fff, #fbfff9);padding:12px;border-radius:12px;border:1px solid rgba(11,42,26,0.04);opacity:0;transform:translateY(10px);animation:show 0.4s ease forwards}
@keyframes show{to{opacity:1;transform:translateY(0)}}

.park-list{display:grid;gap:12px}
.thumb{width:72px;height:72px;border-radius:12px;background:linear-gradient(135deg,#eafff2,#fff0f7);display:flex;align-items:center;justify-content:center;font-weight:700;color:var(--muted)}
.park h3{margin:0;font-size:14px}
.park p{margin:4px 0 0;font-size:12px;color:var(--muted)}
.meta{text-align:right;font-size:12px;color:var(--muted);margin-left:auto}


/* Icono animado del mapa */
.park-icon-bounce{animation:jump 1.6s infinite ease-in-out}
@keyframes jump{0%,100%{transform:translateY(0)}50%{transform:translateY(-4px)}}

/* Footer */
footer{padding:20px;text-align:center;font-size:12px;color:var(--muted)}

/* Responsive */
@media(max-width:980px){main{grid-template-columns:1fr}aside{position:static;height:auto;margin-top:10px}#map{height:60vh}}
